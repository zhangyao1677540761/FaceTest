1 前端发送请求

2 请求传到dispatcherServlet(中央处理器)

3 dispatcherServlet调用handlerMapping(处理器映射器)找到处理器handler

4 handlerMapping返回一个HandlerExecutionchain对象,(包含所有的处理器和拦截器)

5 dispatcherServlet获得HandlerExecutionchain对象后调用handlerAdapter(处理器适配器)

6 handlerAdapter调用handler返回一个modelAndView给dispatcherServlet

7 dispatcherServlet通过视图解析器(因特No锐馊味)返回一个view

8 dispatcherServlet获得view后渲染view，返回一个view给用户