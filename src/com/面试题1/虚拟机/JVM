GC 发生在堆中

分带收集算法
     次数上频繁的Young区  Minor GC
     次数上较少的old区 Full GC
     基本不动Perm区(永久区)

4大算法
    1   引用计数法 (对象有引用就不垃圾回收,无法处理循环引用,淘汰)
    2   复制算法  (Young区 效率高,没有内存碎片,速度快,但是比较占空间)
    3   标记清除  (old区 清除没有标记的对象,
                            会扫描两次(标记一次,清除一次)耗时严重,
                            会产生内存碎片)
    4   标记压缩  (old区,先标记,再压缩
                              会扫描两次(标记一次,压缩一次)耗时严重
                                           内存碎片)
